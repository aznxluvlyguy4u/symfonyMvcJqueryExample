{% extends 'AppBundle:AdminLTE:base.html.twig' %}

{% block body %}
    {% embed 'AppBundle:AdminLTE:_content.html.twig' %}
        {% block content %}
            <h1>Membershipstatuses list</h1>

            <table>
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Label</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for membershipStatus in membershipStatuses %}
                    <tr>
                        <td><a href="{{ path('membershipstatus_show', { 'id': membershipStatus.id }) }}">{{ membershipStatus.id }}</a></td>
                        <td>{{ membershipStatus.label }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('membershipstatus_show', { 'id': membershipStatus.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('membershipstatus_edit', { 'id': membershipStatus.id }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('membershipstatus_new') }}">Create a new membershipStatus</a>
                </li>
            </ul>
        {% endblock %}
    {% endembed %}
{% endblock %}
